// Generated by CoffeeScript 1.4.0
(function() {
  var configureOptions, defaults;

  defaults = {
    processData: false,
    contentType: 'application/json',
    dataType: 'json'
  };

  configureOptions = function(args, type) {
    var callback, data, url;
    url = args[0];
    data = args[1];
    callback = args[2];
    if (typeof data === 'function') {
      callback = data;
      data = null;
    }
    return $.extend({
      url: url,
      type: type,
      data: JSON.stringify(data),
      success: callback
    }, defaults);
  };

  $.extend({
    putJSON: function() {
      return $.ajax(configureOptions(arguments, 'put'));
    },
    postJSON: function() {
      return $.ajax(configureOptions(arguments, 'post'));
    },
    deleteJSON: function() {
      return $.ajax(configureOptions(arguments, 'delete'));
    }
  });

}).call(this);
